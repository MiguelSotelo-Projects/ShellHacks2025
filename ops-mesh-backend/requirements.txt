# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
# sqlite3 is built into Python - no need to install
databases[sqlite]==0.9.0
sqlalchemy==2.0.23
alembic==1.13.1

# Redis
redis==5.0.1
hiredis==2.2.3  # C extension for faster Redis performance

# Retry Logic
tenacity==8.2.3

# HTTP Client (for external APIs/agents)
httpx==0.25.2
aiohttp==3.9.1

# Google ADK/AI Integration
google-cloud-aiplatform==1.38.1
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.2.0

# WebSocket Support (optional for real-time updates)
websockets==12.0

# CORS Support
python-multipart==0.0.6

# Development Tools
python-dotenv==1.0.0
python-json-logger==2.0.7

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2  # For testing FastAPI endpoints
pytest-mock==3.12.0

# Data Validation & Serialization
email-validator==2.1.0
python-dateutil==2.8.2

# Security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
cryptography>=42.0.0

# Async Support
asyncio-mqtt==0.16.1  # If using MQTT for real-time communication
aioredis==2.0.1  # Async Redis client

# Utilities
# uuid is built into Python - no need to install
pytz==2023.3
# pendulum==2.1.2  # Commented out due to Python 3.12 compatibility issues

# Production
gunicorn==21.2.0  # WSGI server for production
prometheus-client==0.19.0  # Metrics (optional)

# Development Only (install with pip install -r requirements-dev.txt)
# black==23.11.0
# flake8==6.1.0
# mypy==1.7.1
# pre-commit==3.6.0